#include <iostream>
using namespace std;

int main(){
	int punsq1[12];
	int punsq2[12];
	int punsq3[12];
	int punsq4[12];
	int punsq5[12];
	int punsq6[12];
	int max = 0, 
	int somsqs[6];

	string nom;
	
	cout << "inserire i punteggi (0, 1, 3) per ognuna delle 12 partite per ognuna delle 6 squadre" << endl;
	
	for(int i = 0; i < 12; i++){
		cout << "inserire punteggio per la partita " << i+1 << " della squadra 1" << endl;
		cin >> punsq1[i];
	}
	for(int i = 0; i < 12; i++){
		cout << "inserire punteggio per la partita " << i+1 << " della squadra 2" << endl;
		cin >> punsq2[i];
	}
	for(int i = 0; i < 12; i++){
		cout << "inserire punteggio per la partita " << i+1 << " della squadra 3" << endl;
		cin >> punsq3[i];
	}
	for(int i = 0; i < 12; i++){
		cout << "inserire punteggio per la partita " << i+1 << " della squadra 4" << endl;
		cin >> punsq4[i];
	}
	for(int i = 0; i < 12; i++){
		cout << "inserire punteggio per la partita " << i+1 << " della squadra 5" << endl;
		cin >> punsq5[i];
	}
	for(int i = 0; i < 12; i++){
		cout << "inserire punteggio per la partita " << i+1 << " della squadra 6" << endl;
		cin >> punsq6[i];
	}
	
	
	for(int k = 0; k < 12; k++){
		somsqs[k] += punsqi+1[k];
	}
	
	for()
	
	
	return 0;
}

